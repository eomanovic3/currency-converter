// import { selectCurrencySelectWidgetDomain } from '../selectors';

describe("selectCurrencySelectWidgetDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
